name: systemaops

services:
  dashboard:
    image: systemaops
    build:
      context: .
      dockerfile: Dockerfile
    container_name: systemaops
    ports:
      - "8080:80"
    restart: always
