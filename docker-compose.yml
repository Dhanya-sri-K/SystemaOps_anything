services:
  dashboard:
    image: systemaops
    build:
      context: .
      dockerfile: Dockerfile
    container_name: understand-anything-dashboard
    ports:
      - "8080:80"
    restart: always
